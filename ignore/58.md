* DOM corruption fix.
* Proper wf-class management from styles panel.
* Init components/File.js from template.
* Nullify selected file on frame timeout (allows clicking again to retry).
* Blue/multi-color progress bar at the very bottom.
* PromptDialog => Default to short, optional long: true.
* Set value menu option.
* Tooltips everywhere (toggleable in settings).
* Tauri version foregoes Companion completely, can be baked-in, much simpler and safer.
  * Will depend on something akin to dnsmasq on Windows and maybe Mac.
* Show all error messages using InfoDialog.
* On ?git= import, if the name already exists offer to choose a different name, merge, or cancel.
* [?] Boo-based terminal maximize over canvas element.
* [?] Detect non-standard HTML files and offer to convert or show in code editor.
